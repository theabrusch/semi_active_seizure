data_gen:
  #
  # Hyperparameters passed to the DataGenerator object
  #
  gen_type: DataGenerator
  hdf5_path: /work3/theb/boston_scalp_new.hdf5
  #hdf5_path: /Users/theabrusch/Desktop/Speciale_data/boston_scalp_new.hdf5
  protocol: train
  window_length: &win_len 2
  stride: &stride [1,1]
  signal_name: EEG
  bckg_rate: &bckg_rate None
  seiz_classes: ['cpsz', 'gnsz', 'spsz', 'tcsz', 'seiz']
  anno_based_seg: True
  prefetch_data_dir: False
  prefetch_data_from_seg: True
  train_percent: 0.9

generator_kwargs:
  batch_size: 64
  val_batch_size: 64


model_kwargs:
  #
  # Hyperparameters passed to the Model.build and __init__ methods
  #
  model_summary: True
  model: &model BaselineCNN


fit:
  #
  # Hyperparameters passed to the Trainer object
  #
  # Loss function
  loss: CrossEntropy 
  weight: *bckg_rate


  # Optimization
  optimizer:
    optimizer: Adam
    lr: 0.001 
    scheduler: None

  n_epochs: 1
  
